pipeline {
    agent any
    stages {
      stage('SCM Checkout') {
            steps {
                echo 'Checkout....'
             }
        }
          stage('Publish Npm') {
                steps {
                    echo 'Publishing on Npm....'
              }
         }
    }
}
