// @flow strict
// values sono i values dei form redux e l undescore prima di values
// serve per evitare che notifichi che la variabile non è utilizzata:)
const validate = _values => {
  const errors = {};
  return errors;
};

export default validate;
