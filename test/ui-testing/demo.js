/* global Nightmare, describe, it, before, after */

module.exports.test = (uiTestCtx) => {
  describe('Module test: ui-cataloging:', function() {
      describe('Start test', () => {
      it('should open app', (done) => {
        console.log('ok');
        done()
      });
    });
  });
};
